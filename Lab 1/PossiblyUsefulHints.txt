import statistics as stmy_list = [1, 2, 3, 4, 6, 5, 8, 7, 6, 4, 6, 3, 2, 5, 7, 4, 2, 4]length = len(my_list) #length is now the length of my listmaximum = max(my_list) #maximum is the highest value in the listminimum = min(my_list) #minimum is the lowest value in the listindex_of_max = my_list.index(max(my_list)) #the index of the highest value in the liststdev = st.stdev(my_list) #standard deviationmean = st.mean(my_list)  #meanmedian = st.median(my_list)  #medianmode = st.mode(my_list) #modedate = "7/21/16"date = date.split("/") #splits the date into a list of day, month and year ["7", "21", "16"]day = int(date[1]) #assigns the integer value of 21 to day